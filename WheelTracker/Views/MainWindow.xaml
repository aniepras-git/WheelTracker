<Window x:Class="WheelTracker.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="WheelTracker Dashboard" Height="600" Width="1000">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Dark" PrimaryColor="DeepPurple" SecondaryColor="Lime" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid>
        <TabControl>
            <TabItem Header="Dashboard">
                <materialDesign:Card Padding="20" Margin="10">
                    <StackPanel>
                        <TextBlock Text="Portfolio Allocation" FontSize="18" Margin="0,0,0,10"/>
                        <lvc:PieChart Series="{Binding AllocationSeries}" Height="200"/>
                        <Button Command="{Binding OpenTradeEntryCommand}" Content="New Trade" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,10"/>
                    </StackPanel>
                </materialDesign:Card>
            </TabItem>
            <TabItem Header="Positions">
                <Frame x:Name="PositionsFrame"/>
                <!-- Host OpenPositionsWindow content -->
            </TabItem>
        </TabControl>
    </Grid>
</Window>